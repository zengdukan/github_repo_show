import { Repo } from "@/lib/data-struct";

type Props = {
  repo?: Repo;
}

export function ProjectInfo(props: Props) {
  return (
    <>
      <div>
        about, topics, count
      </div>

    </>
  );
}